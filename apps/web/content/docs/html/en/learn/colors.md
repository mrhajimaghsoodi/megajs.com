---
title: "HTML Colors"
description: "This page explains using named colors, hex, rgb, and hsl values in HTML examples and CSS with examples, tables, and practical HTML notes."
seoTitle: "HTML Colors Tutorial — W3Schools-style | MEGA JS Docs"
seoDescription: "Learn HTML Colors with HTML examples, syntax tables, common mistakes, and next steps in a W3Schools-style path for accessible pages. Use it in real projects."
keywords: "HTML, HTML Colors, HTML colors, hex RGB HSL, CSS color values"
order: 13
track: learn
sources:
  - name: W3Schools HTML Tutorial
    url: https://www.w3schools.com/html/default.asp
  - name: MDN HTML
    url: https://developer.mozilla.org/en-US/docs/Web/HTML
---
# HTML Colors

HTML Colors focuses on using named colors, hex, rgb, and hsl values in HTML examples and CSS. This is an educational rewrite inspired by the W3Schools HTML learning path, with examples you can edit immediately in the live editor.

When you practice HTML Colors, do not stop at the visual result. Inspect the DOM, check accessibility, name attributes deliberately, and consider the SEO effect of the markup.

## Core idea

- `named colors`: quick readable color names such as tomato.
- `#2563eb`: compact hexadecimal RGB notation.
- `rgb() / hsl()`: functional color formats with numeric control.
- For this topic, matching syntax to the real meaning of the content is the main sign that your markup is doing its job.

## HTML examples

The three examples move from a focused pattern to a complete page. Change one value, text node, or attribute and compare the browser output.

### Example 1: Focused pattern

```html
<section class="colors" aria-labelledby="colors-title">
  <h2 id="colors-title">HTML Colors</h2>
  <p>using named colors, hex, rgb, and hsl values in HTML examples and CSS.</p>
</section>
```

### Example 2: Options in context

```html
<table>
  <caption>HTML Colors quick reference</caption>
  <thead>
    <tr><th scope="col">Syntax</th><th scope="col">Purpose</th></tr>
  </thead>
  <tbody>
    <tr><td><code>named colors</code></td><td>quick readable color names such as tomato</td></tr>
    <tr><td><code>#2563eb</code></td><td>compact hexadecimal RGB notation</td></tr>
    <tr><td><code>rgb() / hsl()</code></td><td>functional color formats with numeric control</td></tr>
  </tbody>
</table>
```

### Example 3: Complete practice page

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HTML Colors practice</title>
  </head>
  <body>
    <section class="colors" aria-labelledby="colors-title">
      <h2 id="colors-title">HTML Colors</h2>
      <p>using named colors, hex, rgb, and hsl values in HTML examples and CSS.</p>
    </section>
  </body>
</html>
```

## Syntax and usage table

| Syntax | Purpose | Authoring note |
| --- | --- | --- |
| `named colors` | quick readable color names such as tomato | In HTML Colors, match it to the real page purpose, not just the visual result. |
| `#2563eb` | compact hexadecimal RGB notation | In HTML Colors, match it to the real page purpose, not just the visual result. |
| `rgb() / hsl()` | functional color formats with numeric control | In HTML Colors, match it to the real page purpose, not just the visual result. |

## Common mistakes

- Using HTML Colors only for appearance without a clear content or accessibility role.
- Copying the example without changing ids, paths, labels, alt text, or visible copy for the real project.
- Forgetting to test `named colors` in the browser, with the keyboard, and in DevTools after the page appears to work.

## Practice step

Create a file named `colors.html`, run the first example, then replace at least one use of `named colors` with content from your own project. Test it with the keyboard and DevTools.

## Next step

Next, continue with **HTML CSS** so the learning path builds on this concept.
